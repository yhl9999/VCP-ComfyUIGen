# ComfyUI 插件配置模板
# 复制此文件为 config.env 并填入实际配置

# ComfyUI服务器地址配置
# 本地部署: http://localhost:8188
# VSCode转发: http://localhost:8188 (端口可能不同)
# 云服务器: http://your-server-ip:8188 或 https://your-domain.com
COMFYUI_BASE_URL=http://localhost:8188

# ComfyUI API密钥（如果启用了认证）
# 大多数情况下本地部署无需密钥，留空即可
COMFYUI_API_KEY=

# 默认工作流模板
# 可选值: text2img_basic, text2img_advanced, img2img_basic, custom
DEFAULT_WORKFLOW=text2img_basic

# 队列检查配置
# 生成图片时会轮询检查队列状态，这些参数控制检查行为
MAX_QUEUE_CHECK_ATTEMPTS=60
QUEUE_CHECK_INTERVAL=3000

# 调试模式
# 设置为true可以看到详细的调试信息
DEBUG_MODE=false